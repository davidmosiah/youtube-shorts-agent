# Security Policy

Do not include OAuth token values, callback codes or private channel/account identifiers in issues. Redact sensitive values before sharing logs.

The default mode is dry-run. Live upload requires `YOUTUBE_DRY_RUN=false`.
